spring:
  application:
    name: aws-secret-manager
  config:
    import: optional:aws-secretsmanager:test-secret?prefix=test.
    secret-prefix: test
  cloud:
    aws:
      credentials:
        access-key: dummy
        secret-key: dummy
      region:
        static: sa-east-1
      endpoint: http://localhost:4566